# v0.1 release-readiness gate

**Candidate revision:** `6c41c68349a052baefc9e44762eb8e633b2e4710` (main, 2026-08-31)

**Gate status:** **NOT READY FOR HUMAN RELEASE DECISION**

This document records the observed v0.1 evidence for Issue #18. It does not
create a release, tag, package publication, merge, or auto-merge. Those actions
require human approval under the
[security and autonomy boundaries](security-and-autonomy-boundaries.md).

## Prerequisite evidence

| Release prerequisite | Observed evidence | Status |
| --- | --- | --- |
| Nine core Skills | `git ls-files 'skills/*/SKILL.md'` listed exactly nine canonical Skills. Their implementation PRs are [#24](https://github.com/boggotron/agentic-engineering/pull/24), [#25](https://github.com/boggotron/agentic-engineering/pull/25), [#26](https://github.com/boggotron/agentic-engineering/pull/26), [#27](https://github.com/boggotron/agentic-engineering/pull/27), and [#28](https://github.com/boggotron/agentic-engineering/pull/28), all merged. | PASS |
| OpenAI adapter | [PR #30](https://github.com/boggotron/agentic-engineering/pull/30) is merged. `adapters/openai/skills` resolves to `../../skills`; `python3 scripts/validate_repository.py` passed, including manifest and symlink validation. | PARTIAL — source package verified; runtime clean-install remains blocked below. |
| Claude adapter | [PR #31](https://github.com/boggotron/agentic-engineering/pull/31) is merged. `python3 adapters/claude/scripts/test_packaging.py` passed (2 tests), and `python3 adapters/claude/scripts/check_shared_content.py` reported 100% files and bytes (9/9 Skills; 27033/27033 bytes). | PARTIAL — package verified; runtime clean-install remains blocked below. |
| CI green | [Validate repository run 33354590080](https://github.com/boggotron/agentic-engineering/actions/runs/33354590080) completed successfully on the candidate revision. Local `python3 scripts/validate_repository.py` also passed. | PASS |
| Behavioral-eval threshold | [PR #33](https://github.com/boggotron/agentic-engineering/pull/33) is merged. `python3 -m unittest evals/test_run.py` passed (8 tests); `python3 evals/run.py --results evals/examples/compliant.json --json` passed 30/30 checks, 100%, above the 90% threshold. | PASS |
| Superpowers parity/deviation review | [PR #35](https://github.com/boggotron/agentic-engineering/pull/35) is merged. [The parity review](superpowers-parity.md) accounts for every workflow Skill at its pinned upstream revision and documents equivalents, deviations, rationales, and host replacements. | PASS |
| Security/autonomy review | [PR #29](https://github.com/boggotron/agentic-engineering/pull/29) is merged. [Security and autonomy boundaries](security-and-autonomy-boundaries.md) preserve the human-only merge, release, and publication controls. This gate change creates no secret, dependency, CI-policy, deployment, or release side effect. | PASS |
| Clean-install tests | Source/package checks above passed. The optional OpenAI plugin-creator validator could not run because its undeclared `yaml` Python dependency is absent. A genuine Codex clean install was not run because the available CLI has no documented disposable configuration root and its plugin commands mutate the active user configuration. A genuine Claude clean install was not run because `claude` is not installed in this environment. | **BLOCKED** |
| Documentation | [PR #34](https://github.com/boggotron/agentic-engineering/pull/34) is merged. README, Codex, ChatGPT, and Claude Code guides, workflow examples, adapter installation guides, and the parity review are present. `rg --files -g '*.md'`, manual changed-link/heading inspection, and repository validation passed. | PASS |

## Checks run for this gate

| Check | Observed result |
| --- | --- |
| `python3 scripts/test_validate_repository.py` | PASS — 6 tests |
| `python3 scripts/validate_repository.py` | PASS |
| `python3 -m unittest evals/test_run.py` | PASS — 8 tests |
| `python3 evals/run.py --results evals/examples/compliant.json --json` | PASS — 30/30 semantic checks, 100% (target: 90%) |
| `python3 adapters/claude/scripts/test_packaging.py` | PASS — 2 tests |
| `python3 adapters/claude/scripts/check_shared_content.py` | PASS — 100% files and bytes, 9/9 Skills |
| `python3 adapters/claude/scripts/package_plugin.py --output /private/tmp/agentic-engineering-claude-release-gate` | PASS — self-contained plugin created for inspection |
| `python3 <plugin-creator>/scripts/validate_plugin.py adapters/openai` | N/A — cannot run: `ModuleNotFoundError: No module named 'yaml'`; installing an undeclared dependency is outside this gate's approved scope. |
| `codex review --uncommitted` | PASS — fresh-context native review found no findings; it confirmed the documentation is internally consistent and changed relative links resolve. |
| Codex OpenAI-adapter clean install | BLOCKED — must use a disposable Codex configuration; no documented disposable configuration root is available in this environment. |
| Claude adapter clean install | BLOCKED — `claude` executable is unavailable. |

## Blocking conditions and remaining risks

The candidate must not be marked `ready-for-human` until both runtime
clean-install procedures have observed evidence:

1. In a disposable Codex configuration, follow
   [the OpenAI clean-install procedure](../adapters/openai/INSTALL.md) against
   a complete checkout, confirm the marketplace listing and installed state,
   then start a new thread and invoke a supplied Skill.
2. In an authenticated environment with Claude Code installed, follow
   [the Claude clean-install procedure](../adapters/claude/README.md#local-development-and-clean-install-check),
   confirm the namespaced Skills in `/help`, and invoke one.

Until those checks pass, the remaining risk is host runtime loading behavior:
the OpenAI adapter's symlink must be preserved by a real installation, and the
generated Claude package must be accepted and expose its namespaced Skills.
The structural checks substantially reduce packaging risk but are not a
substitute for observed host behavior.

## Human release boundary

Once the blockers are resolved, update this evidence, run all applicable checks
again, obtain independent review and green PR CI, and then mark the candidate
ready for human decision. A human must still decide whether to approve and
perform any tag, release, publication, or merge; agents must not do so.
